/** The shortest password Tusker takes. better-auth asks for eight as well. */
export const MIN_PASSWORD = 8;

/** The look of one text field. Every form on the way in uses it. */
export const fieldClass =
  "rounded border border-border bg-surface px-3 py-2";

/**
 * The same field, smaller: the look a control takes beside a title rather than
 * on a line of its own. The org select on the quick-add box is one.
 */
export const smallFieldClass =
  "rounded border border-border bg-surface px-2 py-1 text-xs";
